prompt user to login - if user doesn't exist, prompt the user to enter new information 
new information - firstName, lastName, address, dob, accountType, accountBalance, username & password 
after login - withdraw(overdraft will charge $5 extra-notify user) & deposit 
once complete-display all the transaction with final balance (save new balance)
